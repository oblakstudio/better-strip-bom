build:
  environment:
    node: v18.15
  nodes:
    coverage:
      tests:
        override:
          - command: "npm run test:cov"
            coverage:
              file: coverage/clover.xml
              format: clover
